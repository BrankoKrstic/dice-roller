services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile-nginx
    env_file: .env
    ports: 
      - 443:443
    tmpfs:
      - /tmp
  rust:
    build:
      context: .
      dockerfile: Dockerfile-rust
    env_file: .env
    ports:
      - 3000:3000
    expose:
      - 3000